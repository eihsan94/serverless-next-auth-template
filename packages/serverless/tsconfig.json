{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "@libTypes/*": ["../../lib/@types/*"],
    },
  },
  "exclude": [
    "node_modules"
  ],
  "types": [
    "node",
    "aws-sdk"
  ],
}
